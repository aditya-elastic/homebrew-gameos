# Homebrew Game OS Tap

Install the current Game OS CLI:

```bash
brew tap aditya-elastic/gameos
brew install gameos
```

Install the pinned 0.1.0 release:

```bash
brew install aditya-elastic/gameos/gameos@0.1.0
```

The formula installs the public npm package `gameos`.
